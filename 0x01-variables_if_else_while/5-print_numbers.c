#include <stdio.h>

/**
 * main - Print the numbers from 0 to 9
 *
 * Return: 0 always runs
 */

int main(void)
{
	int a;

	for (a = 0; a < 10; a++)
	{
		printf("%d", a);
	}
	printf("\n");
	return (0);
}
